<div class="container py-4">
  <%= render 'shared/page_header', title: 'Edit Page', subtitle: 'Update the page details' %>

  <div class="card border-0 shadow-sm">
    <div class="card-body p-4">
      <%= render "form", page: @page %>
    </div>
  </div>
</div>
