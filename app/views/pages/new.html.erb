<div class="container py-4">
  <%= render 'shared/page_header', title: 'New Page', subtitle: 'Create a new site page' %>

  <div class="card border-0 shadow-sm">
    <div class="card-body p-4">
      <%= render "form", page: @page %>
    </div>
  </div>
</div>
