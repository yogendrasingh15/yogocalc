<nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top header-navbar">
  <div class="container-fluid px-3 px-md-4 d-flex justify-content-between align-items-center">
    <!-- Brand -->
    <%= link_to root_path, class: 'navbar-brand d-flex align-items-center fw-bold' do %>
      <div class="brand-icon me-3">
        <i class="bi bi-calculator-fill"></i>
      </div>
      <div class="d-none d-lg-block">
        <div class="brand-name">DRY Solutions</div>
        <div class="brand-tagline">Reinvent yourself</div>
      </div>
    <% end %>
    
    <div class="d-flex align-items-center">
      <% if user_signed_in? %>
        <div class="me-3 d-none d-md-block">
          Signed in as <strong><%= current_user.name %></strong>
        </div>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-outline-secondary btn-sm' %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-primary btn-sm me-2' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-outline-primary btn-sm' %>
      <% end %>
    </div>
  </div>
</nav>

<style>
  /* Header Navbar */
  .header-navbar {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    border-bottom: 1px solid #e5e7eb;
    padding: 0.75rem 0;
  }

  /* Brand */
  .navbar-brand {
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .navbar-brand:hover {
    opacity: 0.85;
  }

  .brand-icon {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #2563eb, #3b82f6);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .brand-name {
    font-size: 1.1rem;
    font-weight: 700;
    color: #1f2937;
    letter-spacing: -0.5px;
  }

  .brand-tagline {
    font-size: 0.7rem;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 600;
  }

  /* Navigation Links */
  .nav-link {
    color: #6b7280 !important;
    font-weight: 500;
    transition: all 0.3s ease;
    padding: 0.5rem 0.75rem !important;
    border-radius: 6px;
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .nav-link:hover {
    color: #2563eb !important;
    background: #f3f4f6;
  }

  .nav-link.active {
    color: #2563eb !important;
    background: #eff6ff;
    font-weight: 600;
  }

  .nav-link i {
    font-size: 1.1rem;
  }

  /* Search Box */
  .search-box {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: #f3f4f6;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    padding: 0.5rem 0.75rem;
    transition: all 0.3s ease;
  }

  .search-box:hover,
  .search-box:focus-within {
    background: white;
    border-color: #2563eb;
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  .search-input {
    border: none;
    background: transparent;
    outline: none;
    width: 200px;
    font-size: 0.9rem;
    color: #1f2937;
  }

  .search-input::placeholder {
    color: #9ca3af;
  }

  .search-box i {
    color: #9ca3af;
  }

  /* Notifications */
  .notification-btn {
    position: relative;
    color: #6b7280 !important;
    padding: 0.5rem 0.75rem !important;
    border-radius: 6px;
    transition: all 0.3s ease;
  }

  .notification-btn:hover {
    background: #f3f4f6;
    color: #2563eb !important;
  }

  .notification-badge {
    position: absolute;
    top: 0;
    right: 0;
    background: #ef4444;
    color: white;
    border-radius: 50%;
    width: 20px;
    height: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.65rem;
    font-weight: 700;
  }

  .notification-dropdown {
    min-width: 320px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.12);
    border: none;
    border-radius: 8px;
  }

  .notification-item a {
    border-bottom: 1px solid #f3f4f6;
    padding: 1rem !important;
    transition: all 0.2s ease;
  }

  .notification-item a:hover {
    background: #f9fafb;
  }

  .notification-item a:last-child {
    border-bottom: none;
  }

  .notification-content p {
    margin: 0;
    color: #1f2937;
    font-weight: 500;
  }

  /* User Avatar */
  .user-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    border: 2px solid #e5e7eb;
    transition: all 0.3s ease;
  }

  .user-dropdown-btn {
    padding: 0.5rem 0.75rem !important;
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .user-dropdown-btn:hover {
    background: #f3f4f6;
  }

  .user-name {
    font-weight: 600;
    color: #1f2937;
  }

  /* User Dropdown */
  .user-dropdown {
    min-width: 280px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.12);
    border: none;
    border-radius: 8px;
  }

  .user-dropdown .dropdown-item {
    color: #374151;
    padding: 0.75rem 1rem;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .user-dropdown .dropdown-item:hover {
    background: #f3f4f6;
    color: #2563eb;
  }

  .user-dropdown .dropdown-item i {
    width: 20px;
    text-align: center;
  }

  /* Preferences */
  .preference-item {
    padding: 1rem;
    border-bottom: 1px solid #f3f4f6;
  }

  .preference-item:last-child {
    border-bottom: none;
  }

  .preference-item:hover {
    background: #f9fafb;
  }

  /* Mobile Responsive */
  @media (max-width: 992px) {
    .navbar-collapse {
      margin-top: 1rem;
      padding-top: 1rem;
      border-top: 1px solid #e5e7eb;
    }

    .search-box {
      margin: 0.75rem 0;
      width: 100%;
    }

    .search-input {
      width: 100%;
    }

    .navbar-nav {
      flex-direction: column;
      gap: 0 !important;
    }

    .nav-link {
      padding: 0.75rem 1rem !important;
    }
  }

  @media (max-width: 576px) {
    .brand-tagline {
      display: none;
    }

    .notification-dropdown {
      min-width: 280px;
    }

    .user-dropdown {
      min-width: 250px;
    }
  }
</style>
