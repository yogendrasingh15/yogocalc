<div class="container-fluid p-4">
  <%= render 'shared/page_header', title: 'New Role', subtitle: 'Create a new role for your team', action_button: [{ label: 'Back to Roles', path: roles_path, style: 'outline-secondary' }] %>

  <div class="card border-0 shadow-sm">
    <div class="card-body">
      <%= render 'form', role: @role %>
    </div>
  </div>
</div>
