<div class="container-fluid p-4">
  <%= render 'shared/page_header', title: 'Edit Contact', subtitle: 'Update contact details', action_button: [{ label: 'Back to Contacts', path: contacts_path, style: 'secondary' }] %>

  <div class="row">
    <div class="col-lg-8">
      <%= render 'form', contact: @contact %>
    </div>
  </div>
</div>
